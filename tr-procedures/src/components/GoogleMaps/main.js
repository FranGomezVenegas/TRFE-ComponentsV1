import './google-map.js';
import './styles.js';

document.addEventListener('DOMContentLoaded', () => {
  const mapElement = document.createElement('google-map');
  document.body.appendChild(mapElement);
});
